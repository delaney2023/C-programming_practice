#include <stdio.h>
/**
 * main - prints "Programming is like building a multilingual puzzle
 * Returns: 0 when exited properly, non-zero otherwise
*/
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
