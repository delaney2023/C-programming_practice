#include <stdio.h>
/**
 * main - Entry point
 * Description - display the output of the variable
 * Return: Always 0 (success)
 */
int main(void)
{
	int number1 = 33;

	printf("%d", number1);
	printf("%d\n", number1);
	return (0);
}
